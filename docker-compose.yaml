version: "3.9"

services:
  app:
    build: .
    container_name: forum-app
    ports:
      - "8080:8080"
    volumes:
      - db-data:/app  # persist the app directory (or just the DB)

volumes:
  db-data: